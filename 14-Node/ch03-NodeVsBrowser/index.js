const dns = require('dns');
// node는 세팅을 하지 않는 이상 기본적으로 내장함수인 require를 사용한다.

// es6 이후의 버전
import React from 'react';

var global; 
// 파일 전체에서 변수로 선언

if (true) {
  let global2;
  // if문 안에서만 변수로 선언
}